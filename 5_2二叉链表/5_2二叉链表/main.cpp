
#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

//经常找后继的   用二叉链表
typedef struct BiNode {
	int data;	//	数据域
	struct BiNode *LChild, *RChild;	//指针域   指向左右孩子
}BiNode, *BiTree;	//前面是结点   后面是指向该结点的指针

int main() {




	return 0;
}